#ifndef INTERRUPTS_HPP
#define INTERRUPTS_HPP

void init_idt();  // Function to initialize the IDT

// Declare other interrupt management functions here, e.g., set_interrupt_handler

#endif // INTERRUPTS_HPP
